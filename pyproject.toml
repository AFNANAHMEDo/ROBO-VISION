[project]
name = "robo-vision"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pyserial>=3.5",
    "torch>=2.13.0",
    "ultralytics>=8.4.114",
]
